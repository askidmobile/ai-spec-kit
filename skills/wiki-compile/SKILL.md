---
name: wiki-compile
description: Compile configured sources into a topic-based wiki (topics, concepts, INDEX) per .wiki-compiler.json.
---

# wiki-compile

Invoke the **wiki-compiler** skill and run it for: $ARGUMENTS

> This is a Warp-compatible wrapper around the wiki-compiler skill.
> In Claude Code / OpenCode / Codex the same workflow is the
> `/wiki-compile` slash command.
